// R10: useEffect と後片付け
// 要件: 10 から 1 秒ごとに減るカウントダウン。0 になったら止まり「終了！」と表示。
//       setInterval を使い、後片付け（clearInterval）を忘れないこと。
//       ヒント: setCount((c) => c - 1) の関数形式を使うと依存配列を [] にできる
export default function Countdown() {
  return <p>（未実装）</p>
}
