// R11: fetch と loading / error
// 要件: 最初の1回だけ fetch('/users.json') でユーザーを取り、名前を <ul> で表示。
//       取得中は「読み込み中...」、失敗したら「エラー: {メッセージ}」。
//       Lesson05 を見ずに書けるか挑戦。（response.ok が false なら throw new Error(...)）
export default function UserFetch() {
  return <p>（未実装）</p>
}
