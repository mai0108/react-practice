// R07: state のリフトアップ
// 要件: 親 TwoButtons が count を持つ。
//       子 PlusButton（+1）と 子 PlusTenButton（+10）はそれぞれ押すと親の count を増やす。
//       親が「合計: {count}」を表示する。子は state を持たない。
export default function TwoButtons() {
  return <p>（未実装）</p>
}
